const app=document.querySelector('#app'); let state={jobs:[],statuses:[],forecasts:[]};
const escape=s=>String(s??'').replace(/[&<>'"]/g,c=>({ '&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'})[c]);
async function load(){for(const [k,f] of Object.entries({jobs:'jobs.json',statuses:'check_status.json',forecasts:'forecasts.json'})){try{state[k]=await fetch(`data/${f}`).then(r=>r.ok?r.json():[])}catch{state[k]=[]}}render('overview')}
function overview(){return `<h2>Overview</h2><div class=card><b>${state.jobs.length}</b> currently tracked matching roles. Data is public job information only.</div><div class=card><h3>Privacy boundary</h3><p>Résumé and referral data never load from this public site. Use the local workflows below.</p></div>`}
function jobs(){return `<h2>Open Jobs</h2>${state.jobs.length?`<table><tr><th>Company</th><th>Role</th><th>Location</th><th>Match</th></tr>${state.jobs.map(j=>`<tr><td>${escape(j.company_name)}</td><td><a href="${escape(j.official_url)}">${escape(j.title)}</a></td><td>${escape(j.location||'—')}</td><td>${escape(j.match_score)}</td></tr>`).join('')}</table>`:'<p class=notice>No public matching jobs yet. Scheduled checks will populate this view.</p>'}`}
function companies(){return `<h2>Companies</h2>${state.statuses.length?`<table><tr><th>Company ID</th><th>Status</th><th>Checked</th><th>Matches</th></tr>${state.statuses.map(s=>`<tr><td>${escape(s.company_id)}</td><td>${escape(s.status)}</td><td>${escape(s.checked_at)}</td><td>${escape(s.matching_jobs)}</td></tr>`).join('')}</table>`:'<p class=notice>Check status will appear after the first monitor run.</p>'}`}
function forecast(){return `<h2>Opening Forecast</h2><p class=notice>Forecasts display only verified full-time new-grad observations. Internship history is separate and not mixed in.</p>${state.forecasts.length?`<pre>${escape(JSON.stringify(state.forecasts,null,2))}</pre>`:'<p>No verified observations yet; no estimate is shown.</p>'}`}
function resume(){return `<h2>Résumé Tailoring</h2><p class=notice><b>Local-only workflow.</b> This GitHub Pages site cannot process or retain your résumé. Do not upload private résumé content here.</p><label>Discovered job<select>${state.jobs.map(j=>`<option>${escape(j.company_name)} — ${escape(j.title)}</option>`).join('')}</select></label><p>Run locally: <code>job-watcher tailor-resume --tex ~/resume.tex --job-slug company-role --job-description-file job.txt</code></p><p>The command creates an unchanged valid-LaTeX draft plus <code>changes.diff</code> and <code>analysis.json</code> under ignored <code>generated/resumes/</code>. It identifies evidence and unsupported requirements, but never invents qualifications.</p>`}
function referrals(){return `<h2>Referrals</h2><p class=notice><b>Private/local-only.</b> Contact names, notes, referral history, and outreach drafts are excluded from Pages and Git by default.</p><p>Track company, relevant roles, deadline, referral status, contact name/role/profile URL, relationship, contact and follow-up dates, response, referral submitted state, and notes in <code>private/referrals.json</code>.</p><p>Supported statuses: Not started · Potential contact identified · Connection request sent · Message sent · Follow-up needed · Responded · Referral offered · Referral submitted · Declined · No response · Applied without referral.</p><p>Draft messages are local templates only—never sent automatically and never claim a relationship you did not provide.</p>`}
function settings(){return `<h2>Settings</h2><p>GitHub Pages is static; edit the versioned YAML files or generate a snippet locally.</p><pre>${escape(`candidate:\n  graduation_date: "2026-12"\n  preferred_start_date: "2027-01"\n  target_graduation_years: [2026]\n  target_start_years: [2027]\nnotifications:\n  provider: discord\n  discord:\n    webhook_env: DISCORD_WEBHOOK_URL`)}</pre><p>Companies: <code>config/companies.yaml</code> · Match filters: <code>config/filters.yaml</code> · Notification secret: GitHub repository secret <code>DISCORD_WEBHOOK_URL</code>. Keep the dashboard public-only.</p>`}
function render(view){app.innerHTML=({overview,jobs,companies,forecast,resume,referrals,settings}[view])()}document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>render(b.dataset.view));load();
